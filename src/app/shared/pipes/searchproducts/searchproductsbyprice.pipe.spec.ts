import { SearchproductsbypricePipe } from './searchproductsbyprice.pipe';

describe('SearchproductsbypricePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchproductsbypricePipe();
    expect(pipe).toBeTruthy();
  });
});
